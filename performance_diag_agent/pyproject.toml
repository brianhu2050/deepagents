[project]
name = "performance_diag_agent"
version = "0.1.0"
description = "A standalone agent for performance data diagnosis and analysis, built on the deepagents framework."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "deepagents",
    "deepagents-cli",
    "daytona",
    "langchain-anthropic",
]

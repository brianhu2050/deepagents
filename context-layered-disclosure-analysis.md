# Deep Agents CLI - Context 分层披露机制分析

`deepagents-cli` 采用一种精密的分层机制来管理和披露智能体的上下文（Context）。该系统旨在确保智能体在每次交互中都能高效地利用相关信息，同时通过将上下文划分为不同层级来进行管理。其主要目标包括：

1.  **相关性 (Relevance)**: 为当前任务提供最相关的上下文。
2.  **效率 (Efficiency)**: 优化 Token 使用，避免无关信息过载 Prompt。
3.  **持久性 (Persistence)**: 在不同会话间保持学习到的知识。
4.  **可定制性 (Customization)**: 允许用户在全局和项目级别上定制智能体的行为和知识。

该机制主要由 `AgentMemoryMiddleware` 实现，并利用 `agent.md` 文件和项目特定的内存文件。

## 上下文的层次结构

上下文由以下几个层次构成，每个层次都有其独特的目的和范围：

### 1. 基础系统提示 (Base System Prompt)

-   **描述**: 这是最基础的层次，包含了智能体的核心指令、工具使用规范、约束条件和通用行为准则。它由 `agent.py` 中的 `get_system_prompt` 函数生成。
-   **范围**: 适用于所有交互的全局指令。
-   **内容**:
    -   关于工作环境（本地或远程沙箱）的信息。
    -   使用工具（如 `web_search`, `write_todos`）的详细指令。
    -   如何处理人机回路（Human-in-the-Loop）批准流程。
    -   文件路径处理规则。

### 2. 用户记忆 (User Memory)

-   **文件位置**: `~/.deepagents/<agent_name>/agent.md`
-   **描述**: 此层存储用户的个人偏好、编码风格以及适用于所有项目的通用指令。
-   **范围**: 全局，适用于特定用户的所有项目。
-   **内容**:
    -   智能体的通用语气和沟通风格。
    -   普适的编码规范（例如，使用类型提示、格式化风格）。
    -   与项目无关的通用工作流程。
-   **机制**: `AgentMemoryMiddleware` 在会话开始时读取此文件，并将其内容注入到系统提示的 `<user_memory>` 标签内。

### 3. 项目记忆 (Project Memory)

-   **文件位置**: `<project_root>/.deepagents/agent.md`
-   **描述**: 此层包含仅与当前项目相关的上下文、规则和指令。
-   **范围**: 仅限于当前的 Git 项目。
-   **内容**:
    -   项目特定的架构和设计模式。
    -   仅适用于此代码库的编码规范。
    -   测试策略和部署流程。
    -   团队规范和项目结构。
-   **机制**: 当 CLI 在一个 Git 仓库中运行时，`AgentMemoryMiddleware` 会在项目根目录中查找 `agent.md` 文件，并将其内容注入到系统提示的 `<project_memory>` 标签内。

### 4. 项目特定内存文件 (Project-Specific Memory Files)

-   **文件位置**: `<project_root>/.deepagents/*.md` (例如, `api-design.md`, `architecture.md`)
-   **描述**: 这些文件用于存储结构化的项目知识。智能体被指示在需要时读取和更新这些文件，但它们不会被自动加载到每个 Prompt 中。
-   **范围**: 在当前项目中按需使用。
-   **内容**:
    -   API 设计文档。
    -   架构决策记录。
    -   部署流程。
    -   常见的调试模式。
-   **机制**:
    -   这些文件不会被自动注入系统提示。
    -   相反，基础系统提示会指示智能体在遇到相关任务时，首先检查 (`ls`) 并读取 (`read_file`) 这些文件。
    -   这允许智能体在节省 Token 的同时，按需检索深度上下文。

### 5. 技能 (Skills) - 渐进式披露

-   **文件位置**: `~/.deepagents/<agent_name>/skills/` 和 `<project_root>/.deepagents/skills/`
-   **描述**: 技能为特定任务提供可复用的工作流和领域知识。它们遵循**渐进式披露 (progressive disclosure)** 模式。
-   **范围**: 可在用户或项目级别使用。
-   **机制**:
    1.  **发现 (Discovery)**: `SkillsMiddleware` 在会话开始时解析所有 `SKILL.md` 文件的 YAML frontmatter（名称、描述）。
    2.  **摘要注入 (Summary Injection)**: 技能的名称和描述列表被注入系统提示。这告知了智能体有哪些可用技能，但没有注入全部内容。
    3.  **按需加载 (On-demand Loading)**: 当智能体判断用户请求与某个技能的描述匹配时，它会使用 `read_file` 工具读取相应 `SKILL.md` 文件的全部内容。
    4.  **执行 (Execution)**: 智能体遵循技能文件中描述的详细步骤进行操作。

## 披露流程总结

上下文的披露按以下顺序进行：

1.  **会话启动**:
    -   `AgentMemoryMiddleware` 读取用户和项目的 `agent.md` 文件。
    -   `SkillsMiddleware` 解析所有技能的元数据。

2.  **Prompt 构建 (`wrap_model_call`)**:
    -   加载基础系统提示。
    -   用户和项目记忆被注入到 `<user_memory>` 和 `<project_memory>` 块中。
    -   可用技能列表（名称和描述）被注入。
    -   关于如何使用长期记忆和技能的指令被添加。

3.  **智能体执行**:
    -   智能体接收到这个富含上下文的 Prompt。
    -   它分析用户请求，并基于注入的上下文（记忆和技能列表）制定计划。

4.  **动态上下文加载**:
    -   如果智能体确定需要某个特定技能，它会使用 `read_file` 加载该 `SKILL.md` 的全部内容。
    -   如果智能体需要关于项目的深度信息，它会使用 `ls` 和 `read_file` 从 `.deepagents/` 目录中加载相关的内存文件。

这种分层方法确保了智能体既拥有一个稳定的操作基础上下文，又能根据需要动态加载更深、更具体的知识，这是 `deepagents-cli` 强大而高效的关键架构设计之一。

# Deep Agents CLI - 컨텍스트 계층적 공개 메커니즘 분석

`deepagents-cli`는 에이전트의 컨텍스트를 관리하고 공개하기 위해 정교한 계층적 메커니즘을 사용합니다. 이 시스템은 에이전트가 모든 상호 작용에서 관련 정보를 효율적으로 사용할 수 있도록 보장하며, 컨텍스트를 여러 계층으로 분리하여 관리합니다. 주요 목표는 다음과 같습니다.

1.  **관련성**: 작업에 가장 관련성 높은 컨텍스트를 제공합니다.
2.  **효율성**: 불필요한 정보로 프롬프트를 과부하하지 않음으로써 토큰 사용을 최적화합니다.
3.  **지속성**: 세션 간에 학습된 지식을 유지합니다.
4.  **사용자 정의**: 사용자가 에이전트의 행동과 지식을 글로벌 및 프로젝트별로 사용자 정의할 수 있도록 합니다.

이 메커니즘은 `AgentMemoryMiddleware`에 의해 구현되며, `agent.md` 파일과 프로젝트별 메모리 파일을 활용합니다.

## 컨텍스트의 계층

컨텍스트는 다음과 같은 여러 계층으로 구성되며, 각 계층은 고유한 목적과 범위를 가집니다.

### 1. 기본 시스템 프롬프트 (Base System Prompt)

-   **설명**: 이것은 가장 기본적인 계층으로, 에이전트의 핵심 지침, 도구 사용법, 제약 조건 및 일반적인 행동 가이드라인을 포함합니다. `agent.py`의 `get_system_prompt` 함수에서 생성됩니다.
-   **범위**: 모든 상호 작용에 적용되는 글로벌 지침입니다.
-   **내용**:
    -   작업 환경(로컬 또는 원격 샌드박스)에 대한 정보.
    -   도구 사용에 대한 자세한 지침(예: `web_search`, `write_todos`).
    -   인간-참여(Human-in-the-Loop) 승인 프로세스 처리 방법.
    -   파일 경로 처리 규칙.

### 2. 사용자 메모리 (User Memory)

-   **파일 위치**: `~/.deepagents/<agent_name>/agent.md`
-   **설명**: 이 계층은 사용자의 개인적인 선호도, 코딩 스타일 및 모든 프로젝트에 걸쳐 적용되는 보편적인 지침을 저장합니다.
-   **범위**: 특정 사용자의 모든 프로젝트에 걸쳐 글로벌하게 적용됩니다.
-   **내용**:
    -   에이전트의 일반적인 톤과 커뮤니케이션 스타일.
    -   보편적인 코딩 규칙(예: 타입 힌트 사용, 포맷팅 스타일).
    -   프로젝트에 구애받지 않는 일반적인 워크플로우.
-   **메커니즘**: `AgentMemoryMiddleware`는 세션 시작 시 이 파일을 읽어 `<user_memory>` 태그 안에 시스템 프롬프트에 주입합니다.

### 3. 프로젝트 메모리 (Project Memory)

-   **파일 위치**: `<project_root>/.deepagents/agent.md`
-   **설명**: 이 계층은 현재 프로젝트에만 관련된 컨텍스트, 규칙 및 지침을 포함합니다.
-   **범위**: 현재 Git 프로젝트로 범위가 제한됩니다.
-   **내용**:
    -   프로젝트별 아키텍처 및 디자인 패턴.
    -   해당 코드베이스에만 적용되는 코딩 규칙.
    -   테스트 전략 및 배포 프로세스.
    -   팀 가이드라인 및 프로젝트 구조.
-   **메커니즘**: CLI가 Git 저장소 내에서 실행되면, `AgentMemoryMiddleware`는 프로젝트 루트에서 `agent.md` 파일을 찾아 `<project_memory>` 태그 안에 시스템 프롬프트에 주입합니다.

### 4. 프로젝트별 메모리 파일 (Project-Specific Memory Files)

-   **파일 위치**: `<project_root>/.deepagents/*.md` (예: `api-design.md`, `architecture.md`)
-   **설명**: 이 파일들은 구조화된 프로젝트 지식을 저장하는 데 사용됩니다. 에이전트는 필요에 따라 이러한 파일을 읽고 업데이트하라는 지시를 받지만, 모든 프롬프트에 자동으로 로드되지는 않습니다.
-   **범위**: 현재 프로젝트 내에서 주문형(on-demand)으로 사용됩니다.
-   **내용**:
    -   API 설계 문서.
    -   아키텍처 결정 사항.
    -   배포 절차.
    -   일반적인 디버깅 패턴.
-   **메커니즘**:
    -   이러한 파일들은 시스템 프롬프트에 자동으로 주입되지 않습니다.
    -   대신, 기본 시스템 프롬프트는 에이전트에게 관련 작업이 발생했을 때 이러한 파일을 확인(`ls`)하고 읽도록(`read_file`) 지시합니다.
    -   이를 통해 에이전트는 토큰을 절약하면서 필요할 때 깊은 컨텍스트를 검색할 수 있습니다.

### 5. 기술 (Skills) - 점진적 공개

-   **파일 위치**: `~/.deepagents/<agent_name>/skills/` 및 `<project_root>/.deepagents/skills/`
-   **설명**: 기술은 특정 작업을 위한 재사용 가능한 워크플로우와 도메인 지식을 제공합니다. 이들은 **점진적 공개(progressive disclosure)** 패턴을 따릅니다.
-   **범위**: 사용자 또는 프로젝트 수준에서 사용할 수 있습니다.
-   **메커니즘**:
    1.  **발견**: `SkillsMiddleware`는 세션 시작 시 모든 `SKILL.md` 파일의 YAML frontmatter(이름, 설명)를 파싱합니다.
    2.  **요약 주입**: 기술의 이름과 설명 목록이 시스템 프롬프트에 주입됩니다. 이는 에이전트에게 사용 가능한 기술을 알려주지만, 전체 내용을 주입하지는 않습니다.
    3.  **주문형 로딩**: 에이전트가 사용자 요청이 특정 기술의 설명과 일치한다고 판단하면, `read_file` 도구를 사용하여 해당 `SKILL.md` 파일의 전체 내용을 읽습니다.
    4.  **실행**: 에이전트는 기술 파일에 설명된 단계별 지침을 따릅니다.

## 공개 프로세스 요약

컨텍스트 공개는 다음과 같은 순서로 이루어집니다.

1.  **세션 시작**:
    -   `AgentMemoryMiddleware`가 사용자 및 프로젝트 `agent.md` 파일을 읽습니다.
    -   `SkillsMiddleware`가 모든 기술의 메타데이터를 파싱합니다.

2.  **프롬프트 구성 (`wrap_model_call`)**:
    -   기본 시스템 프롬프트가 로드됩니다.
    -   사용자 및 프로젝트 메모리가 `<user_memory>` 및 `<project_memory>` 블록에 주입됩니다.
    -   사용 가능한 기술 목록(이름 및 설명)이 주입됩니다.
    -   장기 기억 및 기술 사용 방법에 대한 지침이 추가됩니다.

3.  **에이전트 실행**:
    -   에이전트는 이 풍부한 컨텍스트를 가진 프롬프트를 받습니다.
    -   사용자 요청을 분석하고, 주입된 컨텍스트(메모리 및 기술 목록)를 기반으로 계획을 세웁니다.

4.  **동적 컨텍스트 로딩**:
    -   에이전트가 특정 기술이 필요하다고 판단하면, `read_file`을 사용하여 해당 `SKILL.md`의 전체 내용을 로드합니다.
    -   에이전트가 프로젝트에 대한 깊은 정보가 필요하다고 판단하면, `.deepagents/` 디렉토리에서 관련 메모리 파일을 `ls` 및 `read_file`을 사용하여 로드합니다.

이 계층적 접근 방식은 에이전트가 항상 기본적인 운영 컨텍스트를 가지면서도, 필요에 따라 더 깊고 구체적인 지식을 동적으로 로드할 수 있도록 보장합니다. 이는 `deepagents-cli`를 강력하고 효율적으로 만드는 핵심적인 아키텍처 설계입니다.
